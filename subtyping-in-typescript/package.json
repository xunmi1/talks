{
  "private": true,
  "scripts": {
    "dev": "slidev",
    "build": "slidev build --base /subtyping-in-typescript/ --out ../dist/subtyping-in-typescript",
    "export": "slidev export --dark",
    "format": "slidev format"
  },
  "devDependencies": {
    "@iconify-json/mdi": "^1.1.64"
  }
}
